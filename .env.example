# Copy to .env.local and fill in. Never commit real values.
# Must match the API_KEY env var configured on the Cloud Run backend.
VITE_CHAT_API_KEY=
VITE_CHAT_API_URL=https://digital-twin-agent-988262606076.us-central1.run.app
